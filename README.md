# Edunova

Project workspace divided into team ownership areas:

- `frontend/`: UI application (Frontend team)
- `filter_engine/`: FastAPI core & matching logic (Filter Engine team)
- `database_ingestion/`: Database schemas, pgvector setup & data ingestion scripts (Database team)
